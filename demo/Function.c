#include "stdbool.h"
#include <stdio.h>
#include <stdlib.h>

void Function() {

    /* [ 함수 ]
        - 특정한 작업을 수행하도록 작성된 독립된 코드
        - 반복적인 코드를 재사용 할 수 있도록 함
        - 역할에 따른 코드 구분이 용이함
        - 코드 가독성을 높여줌
    */

   printf(" 함수 ");
}
